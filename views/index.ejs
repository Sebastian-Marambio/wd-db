const Tool = require("../models/Tool.model");


<img src="" alt="Nice logo">
<p>Welcome to the Web Developer Tool Database!</p>

<h2>All tools</h2>
<ul>
    <% data.forEach( tool => { %>
        <p><%= tool.toolName %> </p>
    <% }) %> 
</ul>
